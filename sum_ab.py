# Ask the user to enter the two numbers.
a = float(input("Input A and B and press return\nA: "))
b = float(input("B: "))

# Compute the result.
c = a + b

# Show the entered values and the sum.
print(f"You entered A = {a} and B = {b}")
print(f"C = {c}")
